// Clerk Static Configuration for Frontend
window.ClerkConfig = {
  // Leave blank for local single-user mode. Set a real Clerk publishable key
  // in the deployed copy to require authentication.
  publishableKey: ""
};

// Browser-only Google OAuth client ID. This is public by design; never place a client secret here.
window.GoogleConfig = {
  clientId: ""
};
